════════════════════════════════════════════════════════════════
🚀 КОМАНДИ ДЛЯ ДЕПЛОЮ
════════════════════════════════════════════════════════════════

✅ ЩО ЗРОБЛЕНО:
- Вибір адміна тепер ОБОВ'ЯЗКОВИЙ при реєстрації
- Адмін бачить ТІЛЬКИ лідів своїх брокерів
- Створено скрипт автоматичного створення адмінів

════════════════════════════════════════════════════════════════
📋 ШВИДКИЙ DEPLOY (ОДНА КОМАНДА):
════════════════════════════════════════════════════════════════

ssh root@188.245.228.175 "cd /home/pro-part-agent/htdocs/agent.pro-part.online/propart-real-estate-hub && bash DEPLOY_NOW.sh"


════════════════════════════════════════════════════════════════
📋 АБО ПОКРОКОВО:
════════════════════════════════════════════════════════════════

ssh root@188.245.228.175

# Потім на сервері:
cd /home/pro-part-agent/htdocs/agent.pro-part.online/propart-real-estate-hub

# 1. Оновити код
git pull origin main

# 2. Міграція БД
venv/bin/python3 migrate_add_admin_field.py

# 3. Створити адмінів (автоматично)
venv/bin/python3 create_admins.py

# 4. Перезапустити
pkill -f "python.*run.py"
sleep 2
nohup venv/bin/python run.py > logs/propart.log 2>&1 &

# 5. Перевірити
sleep 3
ps aux | grep "python.*run.py" | grep -v grep
tail -20 logs/propart.log


════════════════════════════════════════════════════════════════
🎯 РЕЗУЛЬТАТ:
════════════════════════════════════════════════════════════════

🌐 https://agent.pro-part.online/register

При реєстрації ОБОВ'ЯЗКОВО вибрати адміна:
  [Dropdown: anton_admin або alex_admin]

👤 Адміни створені:
  - anton_admin / sfajerfe234ewqf#
  - alex_admin / dgerifwef@fmso4

📊 Dashboard адміна:
  - Бачить ТІЛЬКИ лідів своїх брокерів
  - У профілі список його брокерів

════════════════════════════════════════════════════════════════

