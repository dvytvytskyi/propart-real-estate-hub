════════════════════════════════════════════════════════════════
🚀 ФІНАЛЬНІ КОМАНДИ ДЛЯ DEPLOY НА СЕРВЕРІ
════════════════════════════════════════════════════════════════

✅ ВСІ ЗМІНИ ВЖЕ ЗАПУШЕНІ В GIT:
  - Додано пагінацію лідів (20 на сторінку)
  - Створено скрипт видалення всіх лідів
  - Покращено UI пагінації

════════════════════════════════════════════════════════════════
📋 ВИКОНАЙТЕ НА СЕРВЕРІ (скопіюйте всю команду):
════════════════════════════════════════════════════════════════

ssh root@188.245.228.175

Потім на сервері виконайте:

────────────────────────────────────────────────────────────────
ВАРІАНТ 1: Автоматичний (без підтвердження)
────────────────────────────────────────────────────────────────

cd /home/pro-part-agent/htdocs/agent.pro-part.online/propart-real-estate-hub && \
git pull origin main && \
bash auto_deploy_cleanup.sh


────────────────────────────────────────────────────────────────
ВАРІАНТ 2: З підтвердженням (безпечніший)
────────────────────────────────────────────────────────────────

cd /home/pro-part-agent/htdocs/agent.pro-part.online/propart-real-estate-hub && \
git pull origin main && \
bash deploy_and_cleanup.sh


────────────────────────────────────────────────────────────────
ВАРІАНТ 3: Покроково (якщо щось не працює)
────────────────────────────────────────────────────────────────

# 1. Перейти до проекту
cd /home/pro-part-agent/htdocs/agent.pro-part.online/propart-real-estate-hub

# 2. Оновити код
git pull origin main

# 3. Видалити лідів
venv/bin/python3 delete_all_leads.py

# 4. Перезапустити додаток
pkill -f "python.*run.py"
sleep 2
nohup venv/bin/python run.py > logs/propart.log 2>&1 &

# 5. Перевірити статус
sleep 3
ps aux | grep "python.*run.py" | grep -v grep
netstat -tulpn | grep 8090
tail -20 logs/propart.log


════════════════════════════════════════════════════════════════
✅ ЩО БУДЕ ЗРОБЛЕНО:
════════════════════════════════════════════════════════════════

1. ✅ Оновиться код з GitHub
2. ✅ Видаляться ВСІ ліди з бази даних
3. ✅ Перезапуститься додаток
4. ✅ З'явиться пагінація в dashboard

════════════════════════════════════════════════════════════════
🎯 РЕЗУЛЬТАТ:
════════════════════════════════════════════════════════════════

🌐 https://agent.pro-part.online
   - Ліди: видалені
   - Пагінація: активна (20 лідів на сторінку)
   - S3: налаштовано для фото/документів

👤 Користувачі:
   - anton_admin / sfajerfe234ewqf#
   - alex_admin / dgerifwef@fmso4
   - admin / admin123
   - agent / agent123

════════════════════════════════════════════════════════════════

