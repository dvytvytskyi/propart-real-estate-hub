# 🔧 Виправлення SSH підключення

## 📊 Діагностика

✅ **Сервер доступний** (ping працює)  
❌ **SSH порт 22 закритий** (Connection refused)  
✅ **CloudPanel доступний** (порт 8443)

## 🎯 РІШЕННЯ

### Варіант 1: Використати CloudPanel (РЕКОМЕНДОВАНО) ⭐

CloudPanel має вбудований термінал, через який можна виконувати всі команди.

#### Крок 1: Відкрити CloudPanel
```
https://188.245.228.175:8443
або
https://hosting.harbourtech.co/
```

#### Крок 2: Увійти в систему
Використайте ваші облікові дані CloudPanel

#### Крок 3: Знайти сайт
1. Перейдіть до списку сайтів
2. Знайдіть `agent.pro-part.online`
3. Відкрийте його

#### Крок 4: Відкрити термінал
- В CloudPanel є вбудований термінал або "Terminal" функція
- Або використайте "File Manager" → "Terminal"

#### Крок 5: Виконати команди
```bash
cd /home/pro-part-agent/htdocs/agent.pro-part.online/propart-real-estate-hub
git pull origin main
# ... інші команди
```

---

### Варіант 2: Увімкнути SSH через CloudPanel

Якщо у вас є доступ до CloudPanel з правами адміністратора:

1. Відкрийте CloudPanel
2. Перейдіть до "Settings" або "System"
3. Знайдіть "SSH Access" або "Server Settings"
4. Увімкніть SSH доступ
5. Налаштуйте порт (зазвичай 22)

---

### Варіант 3: Звернутися до хостинг-провайдера

**HarbourTech** може налаштувати SSH доступ для вас.

**Що сказати провайдеру:**
- "Потрібно увімкнути SSH доступ до сервера 188.245.228.175"
- "Потрібен доступ для користувача root або pro-part-agent"
- "Або налаштувати SSH на альтернативний порт"

---

### Варіант 4: Перевірити альтернативні порти

Можливо, SSH налаштований на інший порт. Спробуйте:

```bash
# Перевірка різних портів
for port in 2222 22022 22222 2200 22000; do
  echo "Перевірка порту $port..."
  ssh -p $port -o ConnectTimeout=5 root@188.245.228.175 "echo 'Порт $port працює!'" && break
done
```

---

### Варіант 5: Використати CloudPanel API/CLI

Якщо CloudPanel має CLI доступ:

```bash
# Можливо доступний через CloudPanel CLI
clpctl ssh:enable
```

---

## 📋 Команди для виконання (після отримання доступу)

```bash
# Перехід до проекту
cd /home/pro-part-agent/htdocs/agent.pro-part.online/propart-real-estate-hub

# Оновлення коду
git pull origin main

# Перезапуск сервісу
sudo systemctl restart propart

# Перевірка статусу
sudo systemctl status propart

# Перегляд логів
sudo journalctl -u propart -f
```

---

## 🔍 Додаткова діагностика

Якщо хочете перевірити доступність портів:

```bash
# На macOS
nc -zv 188.245.228.175 22
nc -zv 188.245.228.175 2222
nc -zv 188.245.228.175 8443

# Або використайте скрипт
./ssh_diagnose.sh
```

---

## 💡 Рекомендація

**Найшвидший спосіб** - використати CloudPanel веб-інтерфейс. Він має всі необхідні функції для управління сервером без SSH.

Якщо SSH критично необхідний для вашої роботи, зверніться до HarbourTech для налаштування.

